/*
Rust follows the convention that, if you have main.rs defined in source directory,
a binary crate with same name as package will be created automatically and main.rs will
be the crate root.
*/

fn main() {
    println!("Hello, world!");
}
